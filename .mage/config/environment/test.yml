#test
deployment:
  user: estebanc
  from: /var/lib/jenkins/jobs/121IT/workspace/
  to: /var/www/html
  excludes:
    - app/cache/*
    - app/logs/*
    - web/bundles/*
releases:
  enabled: true
  max: 10
  symlink: 121it
  directory: releases/121it_releases
hosts:
    - 10.10.1.15
tasks:
  pre-deploy:
  on-deploy:
    - permissionsConsole
    - prepareTestDeployment
    - symfony2/cache-warmup: {env: prod}
    - symfony2/assets-install
  post-release:
  post-deploy: