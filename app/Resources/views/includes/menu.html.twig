	<!-- Static navbar -->
    <div class="navbar navbar-default" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path('index') }}"><img alt="121CustomerInsight" src="{{ asset('bundles/backend/images/logo_blue.png') }}" width="25px;"></a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
          	{% if is_granted('ROLE_ADMINISTRATOR') %}
          		<li class="dropdown">
	            	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin<span class="caret"></span></a>
	            	<ul class="dropdown-menu" role="menu">
	            		<li class="dropdown-header">Servers</li>
	            		<li><a href="{{ path('server') }}"><span class="fa fa-laptop"></span> Server List</a>
	            		<li class="divider"></li>
	            		<li class="dropdown-header">Projects</li>
	            		<li><a href="{{ path('project') }}"><span class="fa fa-folder"></span> Project List</a>
	            		<li><a href="{{ path('projectgroup') }}"><span class="fa fa-folder-o"></span> Group List</a>
	            		<li class="divider"></li>
	            		<li class="dropdown-header">Users</li>
	            		<li><a href="{{ path('user') }}"><span class="fa fa-user"></span> User List</a>
	            	</ul>
	            </li>
          	{% endif %}
            <li class="active"><a href="{{ path('dashboard_homepage') }}">Dashboard</a></li>
            {% if serversMenu is defined %}
	            <li class="dropdown">
	            	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Servers<span class="caret"></span></a>
	            	<ul class="dropdown-menu" role="menu">
	            		<li><a href="#">121WebHost</a>
	            	</ul>
	            </li>
	        {% endif %}
            {% if projectsMenu is defined %}
            	<li class="dropdown">
            	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Projects<span class="caret"></span></a>
	            	<ul class="dropdown-menu" role="menu">
	            		{% for key,groups in projectsMenu %}
	            			<li class="divider"></li>
		            		<li class="dropdown-header">{{ key }}</li>
		            		{% for keyProject,project in groups %}
		            			<li><a href="#" target="_blank">{{ project.name }}</a></li>
		            		{% endfor %}
		            	{% endfor %}
	            	</ul>
            	</li>
            {% endif %}
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
            <li><a href="#" class="pop-bottom" data-container="body" data-toggle="popover" data-placement="bottom" data-trigger="focus"  title="Profile" data-content='{% include '::includes/profileDashboard.html.twig' %}' data-html="true">
		       	<span class="glyphicon glyphicon-user"></span>
		      </a>
		    </li>
		    <li><a href={{ path('logout') }} class="pop-bottom fa fa-power-off" data-container="body" data-toggle="popover" data-placement="bottom" data-trigger="focus"  title="Logout" data-content='{% include '::includes/logout.html.twig' %}' data-html="true"></a>
		    </li>
		    
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>